paths:
  /v1/subs:
    put:
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  active:
                    type: boolean
                required:
                - active
