paths:
  /v1/orders:
    post:
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  status:
                    enum:
                    - PENDING
                    - COMPLETED
                    type: string
